function main () {
  x := "fibonacci";
  return x(5)
};

function fibonacci (n) {
  a := 1; b := 1; temp := 0;

  while (n >= 1) {
    temp := a;
    a := a + b;
    b := temp;
    n := n - 1
  };

  return a
}
